# 🏗️ Arquitectura Visual del Sistema

## 📊 Vista General del Ecosistema

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ECOSISTEMA COMPLETO                             │
│                                                                          │
│  ┌────────────┐      ┌──────────────┐      ┌─────────────────┐        │
│  │  GitHub    │◄────►│ Google Colab │◄────►│  Google Drive   │        │
│  │ (Código)   │      │  (Ejecución) │      │ (Persistencia)  │        │
│  └────────────┘      └──────────────┘      └─────────────────┘        │
│       │                      │                       │                  │
│       │ Clona                │ Usa                   │ Guarda           │
│       ▼                      ▼                       ▼                  │
│  Archivos .py          GPU Gratuita         Modelos + Datos            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 Estructura del Repositorio (GitHub)

```
sign-language-translator/                    ← Raíz del repo
│
├── 📓 SignLanguageTranslator_Colab.ipynb  ← NOTEBOOK PRINCIPAL
│   └─> Abre en: colab.research.google.com
│
├── 🐍 Módulos Python (Raíz - Directamente Importables)
│   ├── enhanced_keypoints_extractor.py   ⭐ 240 features
│   ├── data_augmentation.py              ⭐ 6+ técnicas
│   ├── advanced_lstm_model.py            ⭐ Bi-LSTM + Atención
│   ├── enhanced_config.py                ⭐ Config auto-detecta Colab/Local
│   └── enhanced_training.py              ⭐ Pipeline completo
│
├── 📚 docs/                              ← Documentación
│   ├── COLAB_IMPLEMENTATION_GUIDE.md    (40+ páginas)
│   ├── QUICK_START_COLAB.md             (Inicio rápido)
│   ├── REPOSITORY_STRUCTURE.md          (Esta guía)
│   ├── FINAL_SUMMARY.md                 (Resumen ejecutivo)
│   └── ARCHITECTURE_VISUAL.md           (Diagramas)
│
├── 📦 Configuración
│   ├── README.md                        (Con badge de Colab)
│   ├── requirements.txt                 (Dependencias)
│   ├── LICENSE                          (MIT)
│   └── .gitignore                       (Excluye models/, data/)
│
└── 📂 models/                           ← Solo .gitkeep (modelos en Drive)
    └── .gitkeep

```

**NOTA IMPORTANTE**: Los archivos Python están en la RAÍZ, no en `src/`.  
Esto permite importarlos directamente en Colab:

```python
from enhanced_keypoints_extractor import EnhancedKeypointsExtractor  ✅
# vs
from src.enhanced_keypoints_extractor import ...  ❌ (NO usar)
```

---

## 🔄 Flujo de Trabajo Detallado

```
┌─────────────────────────────────────────────────────────────────────┐
│  PASO 1: Usuario abre notebook                                     │
│  https://colab.research.google.com/github/USER/repo/blob/.../nb   │
└────────────┬────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  PASO 2: Colab ejecuta celda de setup                              │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ # Montar Drive                                               │  │
│  │ from google.colab import drive                               │  │
│  │ drive.mount('/content/drive')                                │  │
│  │                                                               │  │
│  │ # Clonar repositorio                                         │  │
│  │ !git clone https://github.com/USER/repo.git /content/repo   │  │
│  │                                                               │  │
│  │ # Agregar al path                                            │  │
│  │ import sys                                                    │  │
│  │ sys.path.insert(0, '/content/repo')                          │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────┬────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  PASO 3: Estructura de archivos en Colab                           │
│                                                                      │
│  /content/                                                          │
│  ├── drive/                          ← Google Drive montado        │
│  │   └── MyDrive/                                                  │
│  │       └── SignLanguageTranslator/ ← Proyecto persistente        │
│  │           ├── data/               ← Datos capturados            │
│  │           ├── models/             ← Modelos entrenados          │
│  │           └── logs/               ← Logs de entrenamiento       │
│  │                                                                  │
│  └── sign-language-translator/      ← Repo clonado (temporal)      │
│      ├── enhanced_*.py               ← Código Python               │
│      ├── SignLanguageTranslator_Colab.ipynb                        │
│      ├── docs/                                                      │
│      └── ...                                                        │
└────────────┬────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  PASO 4: Imports funcionan                                          │
│                                                                      │
│  from enhanced_keypoints_extractor import EnhancedKeypointsExtractor│
│  from enhanced_training import EnhancedTrainer                      │
│  from enhanced_config import MODEL_CONFIG  # Auto-detecta Colab!   │
│                                                                      │
│  ✓ Código lee de /content/repo/                                    │
│  ✓ Config escribe en /content/drive/MyDrive/SignLanguageTranslator/│
└────────────┬────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  PASO 5: UI de Gradio funciona                                     │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  Tab 1: Captura      Tab 2: Entrena      Tab 3: Traduce    │  │
│  │    ↓                     ↓                    ↓             │  │
│  │  Guarda en Drive    Usa GPU Colab     Lee modelo de Drive  │  │
│  └─────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Diagrama de Componentes

```
┌───────────────────────────────────────────────────────────────────┐
│                    CAPA DE PRESENTACIÓN                           │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │              Gradio UI (SignLanguageTranslator_Colab.ipynb) │ │
│  │                                                              │ │
│  │  ┌──────────┐ ┌───────────┐ ┌───────────┐ ┌──────────┐   │ │
│  │  │ Captura  │ │Entrena    │ │ Traduce   │ │ Gestión  │   │ │
│  │  │  Tab     │ │  Tab      │ │   Tab     │ │   Tab    │   │ │
│  │  └────┬─────┘ └─────┬─────┘ └─────┬─────┘ └────┬─────┘   │ │
│  └───────┼─────────────┼─────────────┼─────────────┼─────────┘ │
└──────────┼─────────────┼─────────────┼─────────────┼───────────┘
           │             │             │             │
           ▼             ▼             ▼             ▼
┌───────────────────────────────────────────────────────────────────┐
│                    CAPA DE LÓGICA DE NEGOCIO                      │
│                                                                    │
│  ┌─────────────────────┐  ┌─────────────────────────────────┐   │
│  │ EnhancedKeypoints   │  │  SignLanguageAugmenter          │   │
│  │   Extractor         │  │  - Temporal augmentation         │   │
│  │ - 240 features      │  │  - Spatial augmentation          │   │
│  │ - Spatial (63)      │  │  - Noise augmentation            │   │
│  │ - Geometric (35)    │  │  - Mirror augmentation           │   │
│  │ - Shape (15)        │  └─────────────────────────────────┘   │
│  │ - Temporal (126)    │                                         │
│  │ - Handedness (1)    │  ┌─────────────────────────────────┐   │
│  └─────────────────────┘  │  EnhancedTrainer                │   │
│                            │  - Load & prepare data           │   │
│  ┌─────────────────────┐  │  - Normalize sequences           │   │
│  │ AdvancedSign        │  │  - Augment data                  │   │
│  │  LanguageModel      │  │  - Split data                    │   │
│  │ - Bi-LSTM (256)     │  │  - Train model                   │   │
│  │ - MultiHead Attn    │  │  - Evaluate model                │   │
│  │ - Residual Conn     │  │  - Plot results                  │   │
│  │ - Layer Norm        │  └─────────────────────────────────┘   │
│  └─────────────────────┘                                         │
└───────────────────────────────────────────────────────────────────┘
           │             │             │             │
           ▼             ▼             ▼             ▼
┌───────────────────────────────────────────────────────────────────┐
│                    CAPA DE DATOS                                  │
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ MediaPipe    │  │ TensorFlow   │  │ Google Drive         │   │
│  │ Hands        │  │ Keras        │  │ - data/raw/          │   │
│  │ - Detección  │  │ - GPU        │  │ - models/*.h5        │   │
│  │ - Tracking   │  │ - Training   │  │ - logs/              │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔀 Flujo de Datos: Captura → Entrenamiento → Predicción

```
┌─────────────────────────────────────────────────────────────────┐
│  CAPTURA DE DATOS                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Webcam → MediaPipe → EnhancedKeypointsExtractor                │
│           Hands       │                                          │
│                       ▼                                          │
│              21 landmarks (x, y, z)                             │
│                       │                                          │
│                       ▼                                          │
│           ┌───────────────────────────┐                         │
│           │  Feature Extraction       │                         │
│           │  ┌─────────────────────┐  │                         │
│           │  │ Spatial (63)        │  │                         │
│           │  │ Geometric (35)      │  │                         │
│           │  │ Shape (15)          │  │                         │
│           │  │ Temporal (126)      │  │                         │
│           │  │ Handedness (1)      │  │                         │
│           │  └─────────────────────┘  │                         │
│           └───────────────┬───────────┘                         │
│                           ▼                                      │
│                 240-dim feature vector                          │
│                           │                                      │
│                           ▼                                      │
│          Sequence (30 frames × 240 features)                    │
│                           │                                      │
│                           ▼                                      │
│              Save: sample_TIMESTAMP.npy                         │
│                           │                                      │
│                           ▼                                      │
│       /Drive/SignLanguageTranslator/data/raw/SIGN_NAME/         │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ENTRENAMIENTO                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Load samples → Normalize → Augment → Split → Train            │
│       │            │           │         │        │             │
│       ▼            ▼           ▼         ▼        ▼             │
│   *.npy files   Pad to      x2-3      70/15/15  Bi-LSTM        │
│                 30 frames   samples    Train/    + Attention    │
│                                        Val/Test                  │
│                                                                  │
│  Data Augmentation (SignLanguageAugmenter):                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • Temporal: Speed variation, temporal cropping           │  │
│  │ • Spatial: Scaling, translation, rotation                │  │
│  │ • Noise: Gaussian noise, feature dropout                 │  │
│  │ • Mirror: Horizontal flip (with handedness correction)   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           ▼                                      │
│         Model Architecture (AdvancedSignLanguageModel):         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Input (30, 240)                                           │  │
│  │     ↓                                                      │  │
│  │ LayerNorm → Conv1D(128) → BatchNorm → Dropout             │  │
│  │     ↓                                                      │  │
│  │ PositionalEncoding                                        │  │
│  │     ↓                                                      │  │
│  │ Bi-LSTM(256) + Residual → LayerNorm → Dropout             │  │
│  │     ↓                                                      │  │
│  │ MultiHeadAttention(8) + Residual → LayerNorm → Dropout    │  │
│  │     ↓                                                      │  │
│  │ Bi-LSTM(128) + Residual → LayerNorm → Dropout             │  │
│  │     ↓                                                      │  │
│  │ GlobalMaxPooling + GlobalAvgPooling → Concat              │  │
│  │     ↓                                                      │  │
│  │ Dense(128) → BatchNorm → Dropout                          │  │
│  │     ↓                                                      │  │
│  │ Dense(64) → BatchNorm → Dropout                           │  │
│  │     ↓                                                      │  │
│  │ Dense(num_classes, softmax)                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           ▼                                      │
│                   model_best.h5                                 │
│                           │                                      │
│                           ▼                                      │
│       /Drive/SignLanguageTranslator/models/                     │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  PREDICCIÓN (TRADUCCIÓN)                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Webcam → MediaPipe → Extract Features → Model → Predict       │
│           (21 pts)     (240-dim)          (.h5)   (softmax)     │
│                                                        │         │
│                                                        ▼         │
│                                              Class probabilities│
│                                                        │         │
│                                                        ▼         │
│                                                  argmax()        │
│                                                        │         │
│                                                        ▼         │
│                                            Predicted sign + conf│
│                                                        │         │
│                                                        ▼         │
│                                               Display in UI      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💾 Gestión de Almacenamiento

```
┌───────────────────────────────────────────────────────────────┐
│  SEPARACIÓN DE RESPONSABILIDADES                              │
├───────────────────────────────────────────────────────────────┤
│                                                                │
│  GitHub                 Google Drive            Google Colab  │
│  (Código)               (Persistencia)          (Ejecución)   │
│     │                        │                       │         │
│     ▼                        ▼                       ▼         │
│  ┌─────────┐           ┌──────────┐           ┌──────────┐   │
│  │ *.py    │           │ *.h5     │           │ Runtime  │   │
│  │ *.ipynb │           │ *.npy    │           │ (GPU)    │   │
│  │ *.md    │           │ *.log    │           │ Temp     │   │
│  │ *.txt   │           │ *.json   │           │ Files    │   │
│  └─────────┘           └──────────┘           └──────────┘   │
│  ~500 KB                Variable               Session-based  │
│  Versionado             15 GB gratis           Reinicia c/12h │
│                                                                │
│  Qué guardar:           Qué guardar:           Qué NO guardar:│
│  ✓ Código               ✓ Modelos              ✗ Nada        │
│  ✓ Docs                 ✓ Datos                               │
│  ✓ Configs              ✓ Logs                                │
│  ✗ Modelos (grandes)    ✗ Código (Git mejor)                 │
│  ✗ Datos (privados)                                           │
└───────────────────────────────────────────────────────────────┘
```

---

## 🔄 Ciclo de Vida del Proyecto

```
┌────────────────────────────────────────────────────────────┐
│  DÍA 1: SETUP                                              │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 1. Crear repo en GitHub                              │ │
│  │ 2. Subir código Python (.py) y notebook (.ipynb)     │ │
│  │ 3. Abrir notebook en Colab                           │ │
│  │ 4. Ejecutar setup (monta Drive, clona repo)          │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│  DÍA 2-3: CAPTURA DE DATOS                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 1. Definir señas (5-10 inicialmente)                 │ │
│  │ 2. Capturar 10-15 muestras por seña                  │ │
│  │ 3. Guardar en Drive/data/raw/                        │ │
│  │ 4. Verificar calidad de capturas                     │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│  DÍA 4: ENTRENAMIENTO                                     │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 1. Cargar datos desde Drive                          │ │
│  │ 2. Aplicar data augmentation (2x-3x datos)           │ │
│  │ 3. Entrenar modelo (GPU de Colab)                    │ │
│  │ 4. Guardar modelo_best.h5 en Drive                   │ │
│  │ 5. Revisar métricas (accuracy, confusion matrix)     │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│  DÍA 5: EVALUACIÓN Y MEJORA                               │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ 1. Probar modelo con traducción en tiempo real       │ │
│  │ 2. Identificar señas con baja precisión              │ │
│  │ 3. Capturar más muestras de señas problemáticas      │ │
│  │ 4. Re-entrenar si es necesario                       │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│  ITERACIÓN CONTINUA                                        │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ • Agregar más señas                                  │ │
│  │ • Capturar más variaciones                           │ │
│  │ • Experimentar con hiperparámetros                   │ │
│  │ • Mejorar augmentation                               │ │
│  │ • Compartir resultados                               │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

---

## 🎯 Puntos Clave de la Arquitectura

### ✅ Ventajas

1. **Separación Clara**
   - Código en GitHub (versionado)
   - Datos en Drive (persistente)
   - Ejecución en Colab (GPU gratis)

2. **Escalabilidad**
   - Fácil agregar nuevas señas
   - Fácil mejorar modelo
   - Fácil compartir con otros

3. **Accesibilidad**
   - No requiere hardware costoso
   - Accesible desde cualquier dispositivo
   - Setup en 3 minutos

### ⚠️ Consideraciones

1. **Sesiones de Colab**
   - Se reinician cada 12 horas
   - Todo en /content/ se borra
   - Por eso usamos Drive para persistencia

2. **Límites de Drive**
   - 15 GB gratis
   - ~600 modelos de 25 MB c/u
   - Suficiente para desarrollo

3. **Límites de Colab**
   - GPU limitada por día
   - Sesiones máximo 12 horas
   - Suficiente para entrenar modelos medianos
